# 测试计划

后续实现至少覆盖：

- 序列与标签长度严格一致；
- unknown残基不会进入损失或指标；
- 同源簇不会跨训练/验证折；
- CAID2/3 accession、完全相同序列和阈值内同源序列会被隔离；
- 模型对长度为`L`的输入只产生一个长度为`L`的分数向量；
- 滑动窗口重建后长度、顺序和重叠权重正确；
- AUC评测与独立成对比较定义一致，包括并列分数；
- 评估器拒绝ID、长度、非有限分数和单类别micro AUC错误。
